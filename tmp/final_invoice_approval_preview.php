<?php
require 'C:/laragon/www/yayasan-app/vendor/autoload.php';
$app=require 'C:/laragon/www/yayasan-app/bootstrap/app.php';$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap();
$accepted=[836=>['unpaid',1774500],838=>['unpaid',50000],839=>['unpaid',50000],841=>['unpaid',75000],842=>['unpaid',75000],843=>['paid',125000]];$quarantine=[837=>['unpaid','paid',1023750,'source unpaid; target paid; tidak ditemukan payment source; status final belum ditetapkan']];$ids=array_keys($accepted+$quarantine);
$targets=Illuminate\Support\Facades\DB::table('payment_invoices')->whereIn('legacy_invoice_id',$ids)->get()->keyBy('legacy_invoice_id');$dupes=Illuminate\Support\Facades\DB::table('payment_invoices')->select('legacy_invoice_id')->whereIn('legacy_invoice_id',array_keys($accepted))->groupBy('legacy_invoice_id')->havingRaw('COUNT(*) > 1')->get()->pluck('legacy_invoice_id')->all();
$rows=[];$errors=[];foreach($accepted as $id=>$v){$t=$targets->get($id);$checks=['exists'=>(bool)$t,'legacy'=>(bool)$t&&((int)$t->legacy_invoice_id===$id),'amount'=>(bool)$t&&((float)$t->amount===$v[1]),'status'=>(bool)$t&&($t->status===$v[0])];if(in_array(false,$checks,true))$errors[$id]=$checks;$rows[]=['legacy_invoice_id'=>$id,'decision'=>'ACCEPT_TARGET_EXISTING','status_final'=>$v[0],'amount'=>$v[1],'target_exists'=>(bool)$t,'target'=>(array)$t,'checks'=>$checks];}$rows[]=['legacy_invoice_id'=>837,'decision'=>'QUARANTINE','status_final'=>null,'amount'=>1023750,'target_exists'=>(bool)$targets->get(837),'reason'=>$quarantine[837][3]];$sumAccepted=array_sum(array_column(array_slice($rows,0,6),'amount'));$out=['generated_at'=>date(DATE_ATOM),'overall_decision'=>(count($errors)===0&&count($dupes)===0)?'READY_FOR_EXPLICIT_RECONCILIATION_APPROVAL':'BLOCKED','accepted_count'=>6,'quarantine_count'=>1,'insert_count'=>0,'accepted_total_amount'=>$sumAccepted,'quarantine_total_amount'=>1023750,'duplicate_accepted_legacy_invoice_ids'=>$dupes,'errors'=>$errors,'payment_migrated'=>false,'payment_status_used_for_invoice_status'=>false,'rows'=>$rows,'initial_quarantine_excluded'=>[292,485,528,529,554,602,654,671,672]];
$dir='C:/laragon/www/yayasan-app/storage/app/audits/sidikma-finance/'.date('Ymd_His').'-final-invoice-approval';mkdir($dir,0777,true);file_put_contents($dir.'/final-invoice-approval-preview.json',json_encode($out,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES));$fp=fopen($dir.'/final-invoice-approval-preview.csv','wb');fputcsv($fp,['legacy_invoice_id','decision','status_final','amount','target_exists']);foreach($rows as $r)fputcsv($fp,[$r['legacy_invoice_id'],$r['decision'],$r['status_final'],$r['amount'],$r['target_exists']?'yes':'no']);fclose($fp);$md="# Final Invoice Approval Preview\n\nDecision: **{$out['overall_decision']}**\n\n- Accepted existing: 6\n- Quarantine: 1 (837)\n- Insert required: 0\n- Accepted nominal: ".number_format($sumAccepted,2,'.',',')."\n- Quarantine nominal: 1,023,750.00\n- Payment table migrated: no\n- Initial quarantine excluded: 9 invoices\n\nInvoice 837 remains quarantined because its source status is unpaid, target status is paid, no source payment was found, and final status is intentionally unset.\n";file_put_contents($dir.'/final-invoice-approval-summary.md',$md);echo json_encode(['dir'=>$dir,'decision'=>$out['overall_decision'],'accepted_total'=>$sumAccepted,'errors'=>$errors,'duplicates'=>$dupes],JSON_PRETTY_PRINT),PHP_EOL;
